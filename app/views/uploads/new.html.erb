<%# story 1 - can upload media& AI maps %>
<section class="d-flex flex-column align-items-center justify-content-start upload-container">
  <div class="m-3 text-center upload-container__heading">
    <h1>📝Upload media</h1>
  </div>

  <%= simple_form_for @upload, html:{class:"upload-form-card p-4 m-3 w-70", style: "max-width: 750px;"} do |f| %>
    <%= f.input :media_type, collection: Upload::MEDIA_TYPES %>
    <%= f.input :file, as: :file %>
    <div class="w-100 d-flex justify-content-center">
    <%= f.submit "Upload", class: "btn btn-primary m-1 w-50" %>
    </div>
  <% end %>
</section>
